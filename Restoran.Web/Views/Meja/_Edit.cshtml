@model Restoran.Model.MstMeja

@using (Html.BeginForm(null, null, FormMethod.Post, new { id = "idFormEditMeja" }))
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>MstMeja</h4>
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.ID)

        @Html.HiddenFor(model => model.KodeMeja)

        <div class="form-group">
            @Html.LabelFor(model => model.KodeTipeRuangan, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownListFor(model => model.KodeTipeRuangan, (IEnumerable<SelectListItem>)ViewBag.ListTipeRuangan, "Pilih Kategori Menu", new { @class = "from-control" })
                @Html.ValidationMessageFor(model => model.KodeTipeRuangan)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.NomerMeja, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.NomerMeja)
                @Html.ValidationMessageFor(model => model.NomerMeja)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.Status, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.Status)
                @Html.ValidationMessageFor(model => model.Status)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
